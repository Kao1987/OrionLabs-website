# Server Configuration
PORT=3001
NODE_ENV=development

# CORS Configuration
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173,https://orionlabs.example.com

# SMTP Email Configuration
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password

# Contact Email (where messages will be sent)
CONTACT_EMAIL=hong.yikao@example.com

# Database Configuration (for production)
# DATABASE_URL=postgresql://username:password@localhost:5432/orionlabs
# REDIS_URL=redis://localhost:6379

# Security
# JWT_SECRET=your-super-secret-jwt-key
# BCRYPT_ROUNDS=12